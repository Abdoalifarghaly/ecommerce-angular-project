import { ScalefotoDirective } from './scalefoto.directive';

describe('ScalefotoDirective', () => {
  it('should create an instance', () => {
    const directive = new ScalefotoDirective();
    expect(directive).toBeTruthy();
  });
});
